pipeline:  
  windows-build:
    image: naviot/scalarizr-build-windows
    group: build
    commands:
      - ls -la
      - pwd
      - rm -rf /ci; git clone -b master --single-branch git@github.com:Scalr/ci.git /ci
      - sleep 10

  centos-build:
    image: centos
    group: build
    commands:
      - export
      - env
      - sleep 10

  ubuntu-build:
    image: ubuntu
    group: build
    commands:
      - export
      - env
      - sleep 10

  ubuntu-build1:
    image: ubuntu
    commands:
      - export
      - env
      - sleep 10


matrix:
  include:
    - OS_NAME: windows
      OS_VERSION: last
    - OS_NAME: centos
      OS_VERSION: 6.7
      OS_ARCH: x86
    - OS_NAME: ubuntu
      OS_VERSION: 14.04
      OS_ARCH: x32
